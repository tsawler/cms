package main

// The site stylesheet is compiled from the templates with the Tailwind
// standalone CLI (brew install tailwindcss — no Node required).
// Regenerate after changing template markup:
//
//	go generate .
//
//go:generate tailwindcss --input assets/input.css --output static/site.css --minify
